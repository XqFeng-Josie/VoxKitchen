# scripts/sweep/pipelines/identity.yaml
# Operator under test: identity (no-op, passes cuts through unchanged).
# Verifies the sweep driver end-to-end against the slim image.
version: "0.1"
name: sweep-identity
work_dir: /tmp/vkit-sweep/identity
num_cpu_workers: 1
ingest:
  source: dir
  args:
    root: /data2/xiaoqinfeng/workdir/VoxKitchen/scripts/sweep/fixtures/audio
stages:
  - name: target
    op: identity
  - name: pack
    op: pack_jsonl
