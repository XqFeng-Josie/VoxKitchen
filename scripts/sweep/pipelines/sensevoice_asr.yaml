# scripts/sweep/pipelines/sensevoice_asr.yaml
# Operator under test: sensevoice_asr (SenseVoiceSmall via FunASR — Chinese).
version: "0.1"
name: sweep-sensevoice-asr
work_dir: /app/work/vkit-sweep/sensevoice_asr
num_cpu_workers: 1
ingest:
  source: dir
  args:
    # Chinese ASR — use audio-zh subdir (zh-tiny.wav).
    root: /app/scripts/sweep/fixtures/audio-zh
stages:
  - name: target
    op: sensevoice_asr
  - name: pack
    op: pack_jsonl
